using System;
using System.Collections.Generic;

namespace Main_Maze_Solver
{

    public class Fill_Maze
    {

        /*public List<List<char>> maze { get; }
        public bool isFinish { get; }

        public Fill_Maze(List<List<char>> maze)
        {
            this.maze = maze;
            this.isFinish = false;
        }

        public void addStep()
        {

        }


        public Point GetStart()
        {
            Point result = new Point();

            for (var eachRow = 0; eachRow < maze.Count; ++eachRow)
            {
                for(var element = 0; element < maze[eachRow].Count; ++element)
                {
                    if(element == 's')
                    {
                        result.X = element;
                        result.Y = eachRow;
                    }
                }
            }
            return null;
        }


        public char Peek_Up(Point point, List<List<char>> maze)
        {
            var newX = point.X;
            var newY = point.Y - 1;

            if (newY < 0)
            {
                return 'x';
            }
            else
            {
                if(maze[newY][newX] == ' ')
                {
                    return ' ';
                }
                return ' ';
            }


            return ' ';

        }*/
    }
}
